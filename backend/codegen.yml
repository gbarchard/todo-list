schema: src/**/*.gql
generates:
  src/generated/resolvers-types.ts:
    config:
      useIndexSignature: true
      avoidOptionals: true
      contextType: src/apollo#RequestContext
      mappers:
        Item: src/resolvers/items/itemModels#ItemModel
    plugins:
      - typescript
      - typescript-resolvers
