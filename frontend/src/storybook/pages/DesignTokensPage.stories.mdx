import { Meta, ColorPalette, ColorItem, Typeset } from "@storybook/addon-docs"

import { StorybookTable } from "../components/StorybookTable/StorybookTable"

<Meta title="Design Tokens/Design Tokens" />

# Design Tokens

Design tokens are the core of what defines our brand; they're the fundamental rules of our style - the colors, typography, spacing, and more.

<h2> Colors </h2>

<ColorPalette>
  <ColorItem
    title="Orange"
    subtitle="Primary"
    colors={{
      "#FFD5AD": "rgb(255,213,173)",
      "#FD8B21": "rgb(253,139,33)",
      "#F27302": "rgb(242,118,2)",
    }}
  />
  <ColorItem
    title="Sacremento"
    subtitle="Secondary"
    colors={{
      "#91CAC3": "rgb(145,202,195)",
      "#008E82": "rgb(0,140,130)",
      "#00665F": "rgb(0,102,95)",
    }}
  />
  <ColorItem
    title="Dark Blue"
    subtitle="Secondary Alt"
    colors={{
      "#CADEF8": "rgb(202,222,248)",
      "#5C90EB": "rgb(92,144,235)",
      "#315BE7": "rgb(49,91,231)",
    }}
  />
  <ColorItem
    title="Gray"
    subtitle="Ternary"
    colors={{
      "#C7CBD1": "rgb(199,203,209)",
      "#555E6D": "rgb(85,94,109)",
      "#31343F": "rgb(49,52,63)",
    }}
  />
</ColorPalette>

<h2> Typography </h2>

export const typography = {
  type: {
    primary: '"DM Sans", sans-serif',
  },
  weight: {
    normal: "400",
    medium: "500",
    bold: "700",
  },
  size: {
    small: 14,
    body: 16,
    h4: 20,
    h3: 24,
    h2: 28,
    h1: 32,
  },
}

export const SampleText = "The mitochondrion is the powerhouse of the cell"

<h3>fonts</h3>
<p>
  Content uses the DM Sans sans-serif font and supports the sizes shown below
  (px)
</p>
<Typeset
  fontSizes={[
    Number(typography.size.small),
    Number(typography.size.body),
    Number(typography.size.h4),
    Number(typography.size.h3),
    Number(typography.size.h2),
    Number(typography.size.h1),
  ]}
  fontWeight={typography.weight.regular}
  sampleText={SampleText}
  fontFamily={typography.type.primary}
/>

export const fontWeightHeaders = ["Name", "Value"]
export const fontWeightRows = [
  ["normal", "400"],
  ["medium", "500"],
  ["bold", "600"],
]

<h3>font weights</h3>
<p> Content is simplified to only three values of font weight. </p>
<StorybookTable headers={fontWeightHeaders} rows={fontWeightRows} />

export const lineHeightHeaders = ["Name", "Value"]
export const lineHeightRows = [
  ["normal", "1"],
  ["medium", "1.2"],
  ["high", "1.5"],
  ["extreme", "1.8"],
]

<h3>line heights</h3>
<p>
  Line height is relative to font size. Below are the line height values used
  across the frontend.
</p>
<StorybookTable headers={lineHeightHeaders} rows={lineHeightRows} />
